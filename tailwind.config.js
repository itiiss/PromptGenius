module.exports = {
  // ... 其他配置
  theme: {
    extend: {
      animation: {
        'spin': 'spin 2s linear infinite',
        'spin-fast': 'spin 1s linear infinite',
      },
    },
  },
  // ... 其他配置
} 